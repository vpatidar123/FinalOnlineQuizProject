import { QuestionsPrimaryData } from './questions-primary-data';

describe('QuestionsPrimaryData', () => {
  it('should create an instance', () => {
    expect(new QuestionsPrimaryData()).toBeTruthy();
  });
});
